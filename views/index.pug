html
  head
    title= title
    style
      include style.css
    meta(name='viewport', content='width=device-width, initial-scale=1')
  body
    //h1= message
    h1 Nate Kerksick
    h3 Workin’ on a dream
    table
      each row in workRows.length ? workRows : ['There are no values']
        tr
          each cell in row.length ? row : ['There are no values']
            td= cell
